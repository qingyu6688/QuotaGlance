# 更新日志

> 文档状态：持续维护  
> 更新日期：2026-07-14
> 文档维护：maorongkang@gmail.com

本文档记录 QuotaGlance 的重要变更。`0.1.3` 是当前面向社区公开的 Latest 软件版本，安装包仍未完成商业签名与 macOS 公证；正式稳定版本目标仍为 `1.0.0`。

版本号遵循语义化版本规则；第一个面向普通用户的正式版本目标为 `1.0.0`，内部开发版本将从 `0.1.0` 开始。

## [Unreleased]

暂无待发布变更。

## [0.1.3] - 2026-07-14

### 变更

- 使用新版周额度卡片与动态水面悬浮球展示图，采用独立资源文件名避免 GitHub CDN 继续命中旧图缓存。
- 再次重写 README 首屏、软件下载、核心体验、使用步骤、常见问题、安全与贡献内容；各平台安装包改为可直接点击的下载链接，源码构建说明收纳为折叠区域。
- Release 工作流继续先创建草稿并校验全部平台资产，校验通过后改为公开非预发布的 Latest Release，确保仓库首页显示实际软件版本而不是仅显示 Tags。
- 项目、npm、Cargo 与 Tauri 版本统一升级到 `0.1.3`。

### 验证

- README 章节、软件下载链接、本地资源、展示图尺寸与 SHA-256 检查通过；项目和标签版本一致性检查通过。
- Release 工作流 YAML 与最终公开步骤的 Bash 语法检查通过，工作流不再包含 `prerelease: true`。
- `npm run check` 通过：ESLint、15 个 Vitest 用例、TypeScript 类型检查和 Vite 生产构建全部成功。
- Rust 格式检查、Clippy 和 45 项测试全部通过。

## [0.1.2] - 2026-07-14

### 变更

- 重写项目 README，以功能、下载、使用、本地开发、安全和贡献为主线，降低首次访问者的阅读成本。
- 删除 README 中的“项目文档”导航和内部交付状态说明，不再把需求、设计、计划等维护文档暴露为项目首页入口。
- 项目、npm、Cargo 与 Tauri 版本统一升级到 `0.1.2`。

### 验证

- README 章节、仓库内链接和“项目文档”残留检查通过，版本一致性检查通过。
- `npm run check` 通过：ESLint、15 个 Vitest 用例、TypeScript 类型检查和 Vite 生产构建全部成功。
- Rust 格式检查、Clippy 和 45 项测试全部通过。

## [0.1.1] - 2026-07-14

### 新增

- 悬浮球支持左键拖拽原生窗口：按住并移动超过 5px 后才启动系统拖动，小幅移动不会误触。
- 浮球无障碍说明补充“按住左键拖动”，并使用 `grab` / `grabbing` 光标反馈当前手势。
- 新增拖拽回归测试，覆盖阈值、单次触发、右键隔离、忙碌状态和拖拽后双击抑制。

### 变更

- 项目、npm、Cargo 与 Tauri 版本统一升级到 `0.1.1`。
- GitHub Release 工作流增加版本一致性、标签一致性、已公开版本不可覆盖、8 个平台资产数量和 SHA-256 逐项校验门禁。

### 修复

- 修复 GitHub Release 校验脚本把输出中的 `SHA256SUMS.txt` 自身作为空文件加入清单的问题，清单现在只覆盖实际安装资产。

### 验证

- `npm run check` 通过：ESLint、15 个 Vitest 用例、TypeScript 类型检查和 Vite 生产构建全部成功。
- Rust 格式检查、Clippy 和 45 项测试通过，Tauri Windows 调试构建成功。
- Windows 原生拖拽烟测通过：136 × 136 浮球实际移动 110px，拖动后仍可双击展开为 320 × 320 卡片；右键退出后 9 个后代进程全部回收，无可见子进程窗口。
- 浏览器回归通过：浮球保持 128 × 128px 正圆、无页面溢出、拖动后组件稳定，控制台无 warning 或 error。

## [0.1.0] - 2026-07-13

### 新增

- 建立项目 README 和文档导航。
- 建立需求、概要设计、详细设计、数据库与本地存储、IPC、测试、部署、安全、隐私、用户手册和故障排查文档基线。
- 建立 `0.1.0` 开发项目计划、里程碑、需求追踪与 UI 设计基线。
- 记录 Windows 本机开发环境与 macOS 待验证项。
- 明确 Codex App Server 为正式数据源，默认通过本地 `stdio` JSONL 通信。
- 明确优先解析 `rateLimitsByLimitId`，同时兼容单桶 `rateLimits`。
- 明确项目为只读额度工具，不调用账号登出、额度重置兑换等写操作。
- 初始化 Git、Tauri 2、React、TypeScript、Vite、Vitest 与 Rust 工程，并加入锁文件、严格类型检查、lint 和统一检查脚本。
- 添加 stable Rust 工具链声明和 GitHub Actions 前端/Windows 桌面检查工作流；远端流水线仍待首次运行验证。
- 实现动态额度桶与窗口领域模型、App Server JSONL 协议层、账号与额度响应解析、错误模型及只读方法白名单。
- 实现 Tauri 应用状态、刷新协调、IPC 命令与事件、卡片/浮球窗口模式、始终置顶、点击穿透、托盘入口和单实例骨架。
- 实现卡片、浮球和设置界面，支持快照 revision 合并、提醒阈值、模式切换、键盘操作与浏览器 mock 数据预览。
- 添加与额度环视觉一致的 QuotaGlance SVG favicon，并生成 Windows、macOS 与通用桌面应用图标。
- 添加 Rust 协议与解析测试，以及前端组件和交互测试。
- 添加仅在 `test-support` 特性下构建的假 App Server，跨进程验证握手顺序、请求 ID、通知、未知服务端请求拒绝、远端错误、畸形输出、超时回收、异常退出和消息上限。
- 实现常驻 App Server 会话、单一 stdin writer/stdout reader、有界 pending request map、通知广播和应用退出清理。
- 将账号与额度通知接入 150 ms 防抖的完整重读；超时请求会从 pending map 移除，连接失败会释放全部在途请求。
- 实现 30 秒自动刷新缓存、应用级 SingleFlight、最后成功快照、可见/隐藏定时重同步、恢复检查和递增退避。
- 实现偏好 JSON 存储：256 KiB 上限、完整字段校验、临时文件替换、有效备份、损坏恢复、未来 Schema 写保护和启动恢复。
- 新增未完成问题分析台账，区分本地缺口、外部 Codex 兼容性、sidecar 分发和发布环境阻塞。
- 生成 Windows x64 Release 预览产物：优化版 EXE、NSIS 安装器、MSI 安装器、SHA-256 清单和限制说明。
- Windows 构建新增 Codex 桌面应用受管运行时发现，在不读取认证文件的前提下复用现有登录态启动只读 App Server。
- 使用当前登录账号与 `codex-cli 0.144.0-alpha.4` 完成真实额度 POC，验证 ChatGPT Pro、多桶、主/次窗口、Credits 和 banked reset 结构。
- 添加 MIT `LICENSE` 文件。
- 公开 GitHub 仓库，补充贡献指南、行为准则、安全报告策略、Issue/PR 模板和 Dependabot 配置，允许社区参与代码、测试、设计、翻译和文档制作。
- README 增加项目展示图、状态徽章、下载入口和 Windows、macOS、Linux 平台说明。
- 增加标签触发的 GitHub Release 流程，分别构建 Windows x64、macOS Apple Silicon、macOS Intel、Linux x64 和 Linux ARM64 安装包；全部平台成功后生成 SHA-256 清单并公开预览版。

### 变更

- 将原始立项分析中的内容拆分为按职责维护的正式文档。
- 将项目状态由“仅有规划文档”更新为“已形成可运行开发基线”，仍将未完成的发布能力明确标注为规划或待验证。
- 参考 `quota-float` 的桌面壳和交互思路，数据链路改为官方 Codex App Server 只读协议，不复制私有 HTTP、固定双窗口或 WebView 轮询实现。
- 前端采用动态 `buckets[]` / `windows[]`，避免把额度模型固定为短周期和周周期两个字段。
- 参考 `quota-float` 重构额度卡片、浮球和设置覆盖层：采用柔和极光渐变、轻量玻璃材质、大数字额度层级、状态化配色与圆角方形浮球，同时保留动态多额度桶和完整错误状态。
- 根据最新额度口径将卡片、浮球和设置收敛为只展示周额度；数据层继续兼容动态窗口，周额度缺失时不回退显示其他周期。
- 浮球新增随周额度百分比升降的动态水面，并为减少动态效果偏好保留静态表现。
- 设置面板升级为“跟随系统、极光、石墨、纸白”四态主题控件，使用 Tabler 图标和独立色样；切换后通过 Tauri IPC 原子保存并在重启后恢复，历史 `light`/`dark` 偏好可兼容迁移。
- 浮球水面重新设计为球体内部液舱：水位随周额度变化，水面持续进行低幅惯性倾斜、位移和回弹，球壳、水体与高光颜色随四套主题联动，并继续遵守系统“减少动态效果”设置。
- 在既有四主题基础上新增日落珊瑚、蜂蜜琥珀、玫瑰铜夜三套暖色渐变主题；设置区改为 4+3 两行居中布局，卡片、边框、球壳、水体和水面高光使用同一套主题令牌。
- 增强浮球液体的高光、暗部、横向位移和回弹关键帧，并为暖色强调色增加高对比前景令牌。
- 将悬浮球由圆角矩形调整为 82px 完整球体：增加玻璃高光、球面明暗、圆形额度环和动态水面，88px 透明窗口只负责阴影留白。
- 按最新浮球参考图将可见球体升级为 128px、原生透明窗口升级为 136px；球内严格采用“周额度、动态百分比、重置日期、状态”四层布局，增加横向水线、右侧气泡、多层球壳光效和低水位文字对比处理。
- 极光、石墨、纸白三套冷色主题新增材质化发光边框：极光使用冰蓝内沿，石墨使用左上青色霓虹热点，纸白使用薄荷/蓝紫底边光；主数字与进度颜色解耦，纸白主题可使用深色主数字。
- 浮球右键改用 Tauri 原生菜单，菜单只保留“设置”和“退出”；设置会展开卡片并打开设置面板，退出通过受控 IPC 结束应用。托盘的刷新、置顶和鼠标穿透恢复项保持不变。
- macOS/Linux 预览版可从 `PATH` 和常见用户安装目录发现已安装的 `codex`；候选路径会规范化并检查普通文件与 Unix 可执行位，子进程仍使用固定 `app-server` 参数且不经过 Shell。

### 修复

- Windows 启动常驻 Codex App Server 时使用 `CREATE_NO_WINDOW`，避免控制台子进程弹出 CMD 黑窗口。
- 修复 `Path` 类型在 macOS/Linux 条件编译中形成未使用导入、导致跨平台 Clippy `-D warnings` 失败的问题。
- 修复 Tauri 透明窗口根节点仍绘制方形底色的问题，使卡片四角完整透出桌面背景。
- 卡片裁切改用 `overflow: clip`，避免主题按钮获得焦点时 WebView 把卡片滚动并裁掉顶部内容。

### 安全

- Tauri WebView 启用严格 CSP 和最小 capability，不授予通用文件、Shell 或网络权限。
- App Server 子进程使用参数化启动而非 Shell 拼接；调试路径要求绝对文件路径，单条 JSONL 消息限制为 1 MiB。
- 正常数据路径不读取 `auth.json`，不向前端传递 Token、原始协议消息或子进程原始错误输出。
- 公开预览版不捆绑或重新分发 Codex App Server，只复用用户已安装的 Codex；正式 `1.0.0` 仍要求固定版本、来源、哈希、许可和签名的受控运行时。

### 测试

- `npm run check` 通过：ESLint、14 个 Vitest 用例、TypeScript 类型检查和 Vite 生产构建全部成功。
- Rust 格式检查、Clippy `-D warnings` 和 45 个 Rust 测试通过，其中包含 9 个跨进程 App Server 契约测试。
- 安装并执行 `cargo-audit 0.22.2`：未发现漏洞，记录 17 个允许的维护或不健全警告供后续依赖升级跟踪。
- 使用 npm 官方 registry 执行依赖审计，结果为 0 vulnerabilities。
- Tauri `--debug --no-bundle` 构建成功，Windows 调试可执行文件完成 3 秒进程烟测。
- Windows Release 真实启动烟测通过：Codex App Server 子进程窗口句柄为 `0`，应用退出后子进程正常回收。
- 浏览器内完成 1200 × 800 与 400 × 720 视口验证：320 px 卡片、88 px 浮球均无溢出或 console error；提醒、置顶、模式切换和 `Escape` 焦点返回通过。
- UI 重构后再次完成 1200 × 800、400 × 720、深色主题、危险额度和设置覆盖层验证；默认双窗口内容无内部滚动，页面无横向或纵向溢出。
- 周额度界面完成 1200 × 800 与 400 × 720 视觉回归：74%/7% 水位联动、波面持续运动、模式切换和控制台检查通过。
- 主题设置完成 1200 × 800 极光/石墨回归：四态单选逐项切换、设置面板无溢出、卡片无内部滚动；动态水面 transform 连续采样变化，确认摇晃动画生效。
- 三套暖色主题完成 1200 × 800 参考/实现同输入视觉回归：七个主题按钮无裁切，第二行三项居中；日落、琥珀、玫瑰铜逐项切换后根节点与 `aria-checked` 同步，强调色前景对比度均不低于 4.5:1。
- 球形浮球与两项右键菜单完成 1200 × 800 浅色/深色及 400 × 720 回归：可见球体 82 × 82px、圆角 50%、菜单仅含“设置”和“退出”，页面无溢出，控制台无警告或错误。
- Windows Release 原生交互烟测通过：菜单实际读取为两项；“设置”可把 88 × 88 浮球展开为 320 × 320 卡片；“退出”结束主进程后无残留 App Server 或 WebView 子进程。
- 四态主题与动态水面版本重新生成 EXE、NSIS、MSI；当前构建启动时 8 个 WebView/App Server 相关子进程均无可见窗口，结束后残留子进程为 0，三个产物的 SHA-256 已同步更新。
- 浏览器设计预览的 `theme` 查询参数改为一次性初始化真实偏好，预览中的选中卡片、实际主题令牌与后续手动切换保持一致。
- 七主题版本重新生成 EXE、NSIS、MSI；真实启动时主窗口句柄非零，8 个 WebView/App Server 相关子进程（含隐藏 `conhost.exe`）窗口句柄均为 0，退出后残留为 0，三个产物的 SHA-256 已刷新。
- 最新浮球参考图完成源图/实现同输入视觉回归；极光、石墨、纸白均在 1200 × 720 预览验证，96% 正常态与 7% 危险态信息、水位和语义色同步，控制台无 warning/error。

### 限制

- 当前 Windows 登录账号和外部 Codex 桌面受管运行时已验证；固定版本兼容范围、完整认证矩阵、macOS 与 bundled sidecar 分发仍未验证。
- macOS、签名、公证、安装器、覆盖升级、卸载与自动更新均未验证。
- 已生成 Windows x64 未签名 Release 预览安装包，但未携带合法 production sidecar，也未通过 Authenticode、Windows 11 安装/升级/卸载验收；本轮仍归入 `Unreleased`，禁止作为正式版本发布。

### 待办

- 使用经批准的专用账号完成 `CodexAppServerProvider` 实机 POC，并补充通知、异常与恢复路径验证。
- 确定随包 sidecar 的版本、来源、SHA-256、目标架构和再分发许可方案。
- 安装并执行 `cargo-audit`，复核 RustSec 结果。
- 完成 Windows 与 macOS 的签名安装器、升级、卸载和更新验收。

## 维护约定

每个版本按以下分类记录：

- `新增`：新功能或新文档。
- `变更`：已有行为或设计调整。
- `修复`：缺陷修复。
- `安全`：安全与隐私相关变更。
- `移除`：删除的功能或兼容能力。

不得把仅在计划中的内容记录为已经交付。正式发布条目必须包含发布日期，并与安装包、Git 标签和应用版本保持一致。
